/// @description Level exit — apprentices touch this to count as saved / exited.
///
/// Uses the object's sprite for draw and collision bbox (scale in the room if needed).
/// Touch is detected from obj_apprentice via core_try_exit().

image_speed = 0;
exit_saved_count = 0;
